# DRD Reticulum Bridge — environment config
# Copy this file to .env and fill in real values before running start_bridge.sh

# FastAPI server base URL (no trailing slash)
API_BASE_URL=http://127.0.0.1:8000

# Service-account JWT — generate with:
#   POST /api/v1/auth/login  →  use a coordinator account
#   copy the access_token from the response
API_TOKEN=

# WebSocket broadcast URL (the /ws/events endpoint on the same server)
WS_URL=ws://127.0.0.1:8000/ws/events

# Reticulum app name and aspect — must match on all bridge nodes in the network
RNS_APP_NAME=drd
RNS_APP_ASPECT=bridge

# Path to the directory that contains the Reticulum 'config' file
# Leave blank to use the default ~/.reticulum
# RNS_CONFIG_DIR=/path/to/reticulum-config

# Logging level: DEBUG | INFO | WARNING
BRIDGE_LOG_LEVEL=INFO
